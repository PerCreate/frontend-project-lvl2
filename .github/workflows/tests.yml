# This is a basic workflow to help you get started with Actions

name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: jest-test
        run: npm run test
        
      - name: linter-test
        run: make lint
